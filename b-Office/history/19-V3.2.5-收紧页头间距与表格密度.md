# MercuryDock 3.2.5 真正收紧页头间距与表格密度

3.2.4 误把行距设成命令集同款 `Padding(8,4)` + 列表顶距 8，视觉上几乎没变紧。
本版按「缩小」落地：

- 状态行移到底部，顶栏与表格之间不再夹状态；列表 `Margin.Top=2`，页边距 `6`
- 表格正文字号 `Small(11)`（单元格 CellTemplate 显式写入，避免 DisplayMemberBinding 不继承）
- 行距 `Padding(4,1)`；表头高 `20`；图钉列字号/高度一并压小
- 选中仍为淡黄底深字
