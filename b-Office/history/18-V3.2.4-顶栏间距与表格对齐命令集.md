# MercuryDock 3.2.4 顶栏间距与表格对齐命令集

## 改动

对照宿主命令集页（`McpToolsView`）：

- 页边距 `12 → 10`；去掉顶栏/状态多余 bottom margin；列表 `Margin.Top=8`
  （与 `ToolList Margin="0,8,0,0"` 一致），收紧页头与表格间距。
- 行距对齐 `Shell.Item.Base`：`Padding=(8,4)`，去掉此前更紧的 `MinHeight=22`。
- 正文字号继续 `Shell.Font.Body`（13）；表头对齐 `Shell.GridHeader`：
  Small 字号、高 26、左对齐、SurfaceAlt 底 + 发丝 gripper。
- 选中仍为淡黄底深字（自绘模板），不回退系统白底。
