# HistoryMercury Verification Contract

## Required Checks

项目合同校验已随宿主 5.0 迁进宿主的进程内开发管线
（`HistoryVulcan.Services.Development.Pipeline.ProjectContract`，由 `vulcan.dev.*` / `vulcan.release.cycle` 调用），
Diana 那份共用脚本不再存在，因此本仓没有可独立运行的合同入口，`commands.verify` 为 `null`。

```powershell
dotnet build .\b-Code-MercuryDock\HistoryMercury.csproj -c Release -p:NuGetAudit=false
dotnet run --project .\b-Code-Tests\HistoryMercury.Smoke\HistoryMercury.Smoke.csproj -c Release -p:NuGetAudit=false
dotnet format .\b-Code-MercuryDock\HistoryMercury.csproj --verify-no-changes --no-restore
powershell -NoProfile -ExecutionPolicy Bypass -File .\b-Code\Build-HistoryMercuryPackage.ps1
```

REQ-MERC-463 adds a source-layout gate: production code remains under the responsibility folders in
`b-Code-MercuryDock`; Smoke lives in `b-Code-Tests/HistoryMercury.Smoke`; version documents live directly in
`b-Office/history`. Active manifests and Diana's release validation must not reference the retired nested
`b-Code-MercuryDock/tests` or `b-Code-MercuryDock/docs` paths.

4.5.1 adds shell-convergence regressions to the smoke gate: an unchanged shortcut folder must report `Written=0` / `Removed=0` / `Unchanged=1` and `Changed=false`, a changed managed field must still rewrite in place, and `ResolveShortcutIntent` / `SanitizePins` must never place one project in both `Pins` and `Excluded`. Registration idempotence is verified against the live per-user registration (`Changed=false`, no registry write, no shell notification).

The smoke gate verifies the module identity, direct 31-command catalog, lowercase `mercury` command names, absence of legacy aliases, the mutable `HistoryMercury` shortcut data root outside `Modules`, Explorer identity (`HistoryClio 项目`), HistoryClio library-root coercion and malformed-setting type fallback, managed shortcut synchronization, registration-owned `registry.domains` / `mercury.projects` / `mercury.dock.commands` annotations, quote-aware dock-command normalization, shortcut file resolution/registration, strict HV executable fallback, dock-entry add/remove handlers, and the real `MercuryShortcutModule` owner lifecycle.

5.0.0 replaces the 4.7.0 BAML gates with stronger ones, because there is no BAML left to inspect: the module must ship
**no** compiled XAML at all (`HistoryMercury.g.resources` must not exist), exactly one public `IModuleContextAware`
entry type which must also be `IDisposable`, and no type may implement any of the host UI lifecycle contracts deleted in
HistoryVulcan 5.0. Constructing the module must not put the dock on the desktop, and `Dispose` must be re-entrant.

5.0.0 also gates the text protocol, which has no compiler behind it: `mercury.ui.describe` and `mercury.ui.actions`
must parse as JSON and satisfy Aurora's own checks (schemaVersion, owner, lowercase unique page ids, every page-level
action id declared, every declared `mercury.*` command actually registered), the `entries` view must carry exactly the
columns the description declares, the three `mercury.ui.*` commands and `mercury.dock.run` must declare a
`HiddenReason`, and `mercury.dock.run` must reject both a key that is not in the dock and raw command text.

5.0.1 adds three gates to the description, one per tightened Aurora rule. All three failure modes are silent —
the page keeps rendering and the buttons keep looking like buttons:

- **No retired page node types.** `button` / `input` / `select` stopped being page nodes in Aurora 1.8.14;
  5.0.0 shipped seven `type: "button"` nodes and every one of them renders as a retirement notice on 1.9.x.
- **No `view` on tables.** `filterable` / `sortable` / `selection` parse but have no implementation anywhere in
  Aurora (its REQ-UI-054 books this as a debt). Declaring them promises filtering that will never happen.
- **Row-action placeholders must be columns the entries view actually produces.** A mismatch does not fail —
  it sends an empty value onto the bus, so the symptom is "clicked it, nothing happened", with no log on either
  side. This one can only be checked after the data view has produced a row, so it lives in the data block.

5.0.2 adds three more description gates, all of them on the dock manager page (REQ-MERC-502):

- **No `text` node on the manager page.** The caption line is gone; the page does not scroll, so that line cost a
  table row.
- **No `lastopened` column.** The column is off the table while the entries view still produces it — row-action
  placeholders read the clicked row's like-named column, so dropping it from the data would silently send an empty
  value onto the bus.
- **No inline row action.** Aurora only builds the trailing 「操作」 column when at least one row action is inline,
  and `inline` defaults to true on Aurora's side, so "not declared" counts as a violation too.

Not gated by smoke, and only verifiable on the running host: the module's effective surface is the **installed
runtime package**, not the committed source. 5.0.1 had the V3 panel rewrite committed but never published, and the
running package still declared flat `widgets`, which Aurora 1.9.2 renders as an empty board. After `vulcan.dev.submit`
confirm with `--runtime vulcan.module.list` that HistoryMercury's version/instanceId actually changed and that
HistoryAurora's command count is still non-zero.

Also gated by the manual pass, not by smoke: the dock's pinned marker is now a ring on the tile rather than a
`·` after the number, and the tile keeps a 2px border in both states so the glyph font size does not change when
a project is pinned.

## Formal Promotion

```powershell
HistoryVulcan.Cli.exe --cli vulcan.dev.submit name=HistoryMercury worktree=<start返回的工作区>
```

正式发布随宿主 5.0 迁进宿主的进程内管线：登记表在 `2026-023-HistoryVulcan/b-Code-Eng/pipeline/module-publish.manifest.json`，
HistoryMercury 已在册（`kind=module`，验证步骤是本仓 Smoke）。管线原子更新 `z-Publish` 并把旧快照留在 `z-Publish/history`。
Diana 那份 `Publish-OneHistoryModule.ps1` 已不存在。

## Runtime Check

`vulcan.command.list` must contain all 31 `mercury` commands, including `mercury.go`, the three `mercury.ui.*`, `mercury.dock.add/remove`, `mercury.shortcut.open/add`, and compatibility `mercury.shortcut.wakeconsole`. `aurora.ui.reloadpages` must report two Mercury pages built and `aurora.ui.missing` must list no Mercury component; every button on the manager page must act (an unbound action renders as an explicit placeholder, which is the failure this protocol exists to make visible). Source acceptance is `HistoryMercury 5.0.0` on host `HistoryVulcan 5.1.0`; the Explorer entry must read `HistoryClio 项目`, the dock tile `HC`, and the project scan must use HistoryClio. After installation, `vulcan.module.reload` and a full host restart must both keep HistoryMercury loaded; no mutable file may appear below its runtime package slot. 4.7.0 adds two dock checks. Independence: with the frontend hidden or never opened, the desktop dock must still be on screen, and its `HC` tile must bring the frontend up; the manager page is a frontend page and is expected to be absent while the frontend is closed. Reload: across a `vulcan.module.reload` the host process must never hold more than one dock window, and must hold exactly one once the reload completes, with `[dock] 桌面坞已就绪` in the log. The host tears the old package down before it builds the new one, so a brief no-dock window (~0.3s measured) is expected and is not a defect. This is runtime-only; a headless smoke cannot create windows and therefore cannot cover it.

## Recent release

- 2026-08-14 (4.5.1 formal release): Diana ran the deploy-then-commit pipeline from a dirty worktree, rebuilt the
  candidate, reran the aligned module contract and 26-command Smoke, atomically promoted `z-Publish` in place while
  the formal host stayed running, archived 4.5.0 to `z-Publish/history`, hot-reloaded the module through
  `vulcan.module.reload`, and refreshed the 111-command system manual. Release build completed with 0 warnings/errors;
  `dotnet format --verify-no-changes` and `git diff --check` passed. All three formal SHA-256 entries were independently
  verified: `HistoryMercury.dll` `D666340882BD7599588F79BAF18ECFFF4D593F24719F85B00180B19A3F5CD118`,
  `HistoryMercury.xml` `163D432CDA04EFCFA8A0C863D08E65A5D8C5792EC82A0479F232491D33D186F4`,
  `module.manifest.json` `AE252A7C509D6D53E1A36D7A6FA3E95CCBE87D6369D9AB001481B790F89DB1D9`.
  Shell convergence measured before the fix: one `SHCNE_ASSOCCHANGED` costs the main `explorer.exe` about 20ms CPU
  (50 broadcasts `+0.92s`, 100 broadcasts `+2.09s`), and every folder-window process pays again while `SeparateProcess=1`.
  Measured after the fix on the running host: five live `mercury.proj.refresh` calls rewrote 0 of 12 `.lnk` files, wrote
  nothing to the registry, added `0.00s` of `explorer.exe` CPU, and started no new shell process.

- 2026-08-13 (4.5.0 formal release): Diana rebuilt the clean-source candidate, reran the aligned module contract and 26-command Smoke, atomically promoted `z-Publish` and the consumer-document mirror (`sourceDirty=false`, source commit `d46497520`), archived 4.4.0, and refreshed the 111-command system manual. Release build completed with 0 warnings/errors; `dotnet format --verify-no-changes` passed. All three formal SHA-256 entries were independently verified: `HistoryMercury.dll` `8849571A4E1F557B44AF24641605BD637F7C24F7B9FFE66074714D4E296F44B9`, `HistoryMercury.xml` `A8381EC857406EBA9CFC5CE4FE70921C6F9C4CFA9E9451A989236E1D25261056`, `module.manifest.json` `BF1E2578AAC12A93C5873993BB974E2AD5B4849B52603A57414D03E9C02640A2`. The manager page is a Shell-resource-backed XAML view with a unified filtered table, single add menu, responsive compact details, and explicit policy popup; `mercury.dock.add/remove` passed positional-parameter, normalization, deduplication, dynamic-provider, and missing-entry failure checks.

- 2026-08-12 (4.4.0 formal release): Diana rebuilt the dirty-source candidate with explicit approval, reran the aligned module contract and 24-command Smoke, atomically promoted `z-Publish` and the consumer-document mirror, archived 4.3.1, and refreshed the 105-command system manual. All three formal SHA-256 entries were independently verified. A restarted HistoryVulcan 3.11.1 service and frontend both logged `HistoryMercury 4.4.0 (24 条指令)` from the formal Z.

- 2026-08-12 (4.4.0 source candidate): project contract, Release build (0 warnings/errors), 24-command Smoke, `dotnet format --verify-no-changes`, and `git diff --check` passed. Focused `go ` detail lookup resolves to `mercury.go`; completion providers come from registration annotations; generic shortcut-file open/add commands and strict HV frontend fallback are covered. `Build-HistoryMercuryPackage.ps1` generated `z-Publish`; manifest version 4.4.0 and all three `SHA256SUMS` entries were independently reverified before formal promotion.

- 2026-08-11 (4.3.1 formal release): project contract, Release build (0 warnings/errors), Smoke (22 commands),
  `dotnet format --verify-no-changes`, and `git diff --check` passed. The focused-domain regression proves `proj.`
  under `janus` advances to `proj.commit` / `proj.list` and then registered parameter metadata. The candidate package
  was generated at `z-Publish`; Diana then atomically promoted Z and the consumer-document mirror,
  archived 4.3.0, and refreshed the 99-command system manual.

- 2026-08-11 local (4.3.0 candidate): project contract passed; `dotnet format --verify-no-changes` and
  `git diff --check` passed. Release build first passed with an isolated intermediate/output directory while the
  standard `obj/Release/net8.0-windows/ref/HistoryMercury.dll` was locked by the running Vulcan Z host; after the
  running Z host processes were stopped, the standard package build also completed. Smoke passed in the current
  worktree:
  `HistoryMercury.Smoke: PASS` (19 mercury commands, direct registration source, shell UI, Explorer shortcut
  folder, global shortcuts, domain focus). `Build-HistoryMercuryPackage.ps1` created the candidate package at
  `z-Publish` with SHA-256 inventory. No formal Z promotion was performed.

- 2026-08-10: Added a regression for the real `MercuryShortcutModule`: its `focus-console` registration is
  released by the owner registrar and can be registered again after reload without a prefix conflict; Smoke passed.

- 2026-08-10: Diana formally promoted HistoryMercury 4.2.0 after the local publisher was replaced by
  `Build-HistoryMercuryPackage.ps1`; Release build completed with 0 warnings and 0 errors, project contract and
  `HistoryMercury.Smoke` (19 commands) passed, and z plus the Diana consumer-document mirror were promoted atomically.
- 2026-08-09: The previous local publisher promoted HistoryMercury 4.2.0 to `z-Publish`; that entry is historical evidence only. Future formal promotion is owned by Diana.

5.0.4 候选验证：在指定 F 盘工作区运行 Smoke 与宿主 submit 门禁，再用 Diana host.observe 比较更新前后。正式并回仍走经审批的 finish。

## 2026-09-13 Diana 真实更新验收（5.0.4）

- 用户授权任选模块验证 Diana。Mercury 仅更新补丁版本与开发文档，未改业务代码或界面协议。
- Smoke PASS（31 commands）；format verify 与 diff check 通过。submit run=20260913-181610-HistoryMercury
  门禁通过并返回「已热重载到活宿主」。未运行旧独立打包脚本。
- Diana 2.6.0 host.observe 基线比较：仅 Mercury 实例由 a8e6503c929f4a6eb6f2bb3ebc728b01
  变为 d0f813b2d33a4b28867a94a819b619be，31 条命令保持不变；其他模块无差异、无退化，
  frontendChanged=false、logsComplete=true、0 新增 Error、issues=[]。
- 经 Diana relay 调用 mercury.app.status 成功，Registered=true。截图仅验 Diana 捕获当前前端的能力，
  未切换 Mercury 页面、未测试右键或快捷键交互；本次文档补丁不据此宣称 UI 全覆盖验收。
- 候选尚未 finish，正式发布文档仍来自原主线；工作区文档由 submit 随候选生成。

最终复核补记：第二次记录提交后，18:19:39 的前端日志出现 6 条面板未声明动作错误，涉及
mercury.entry.refresh / policy / addproject / addcommand / addshortcut / pickshortcut。
动作在 MercuryPages.ActionsJson 中均有声明，本轮未修改该文件。18:21:27 增量读取没有继续报错，
但未执行交互验证，不能证明问题恢复。更新安装/模块附着判据通过，UI 动作功能验收保留未通过；不执行 finish。
首次 18:16:30 的零错误只代表当次区间，不覆盖这次后续重载。

## 2026-09-13 继续升级后的验收结论

本轮两项失败已修复并通过针对性验收，以下结论替代上轮“未解决”状态；不代表所有模块业务交互均已验收。

- ISSUE-AI-001 已解决：Portunus 1.0.6 的 BuildCommandText 改用 CommandParser.QuoteArg。真实 MCP 直调及 relay 的中文 Base64 均为 5Lit5paH；字面量 Unicode 转义仍为 XHU0RTJEXHU2NTg3；引号、反斜杠、CRLF、制表与 emoji 混合输入保真。Portunus Contracts 36/36 通过，全仓格式检查通过（修正一处既有 McpCommands 缩进）。
- Mercury 动作错误已解决：Aurora 1.21.1 单模块 ReloadOwnerAsync 先拉动作再建页，清除旧 owner 动作并刷新目录缓存，保留其它 owner。基于已提交的 1.21.0 快进开发，未覆盖状态按钮和表格更新功能。真实面板回归测试覆盖动作改名/撤销；Contracts 372/372、质量门禁及修改文件格式检查通过。全仓格式检查存在既有未修改文件空白问题，未记为通过。
- 实测 run=20260913-184418-HistoryMercury，Mercury 5.0.4 热重载后仅 Mercury 实例变化，命令保持 31，Aurora 1.21.1 保持 63，8/8 模块附着。18:44:43 observe：errorCount=0、regressionCount=0、logsComplete=true、frontendChanged=false。日志 seq83 actions → seq85 describe，证实单模块重拉顺序恢复。
- 运行环境存在其它并行更新：第一次 Portunus submit 回执后运行区曾回到 1.0.5，不能以回执冒充新代码验收；再次 submit 后已核对 1.0.6、实例及真实结果。Diana 使用方应结合实际版本、实例、日志和 CLI 回执验收。
- 图形能力再次实际读取 PrintWindow 1279x836 PNG，SHA256=3F77ABB53F8BEFAAEFB3FCBD4A4B8F9F50097110726D57407FC7DB519AF65167。截图展示 Janus，不能代替 Mercury 全部交互验收。Juno 管理端不可达仍有独立 Warn，未启动外部服务。
- 保留 Diana 2.6.0、Mercury 5.0.4、Portunus 1.0.6、Aurora 1.21.1 为候选，未执行 finish、push 或正式发布。


## VERIFY-SCENE-REG

核对页面声明的 scene 与模块 owner 一致；热重载前后比较当前场景、可见页、分栏与比例，并确认其他场景的保存布局没有被改写。本轮验证结果待填。
